import { PhysicsSystem } from "../../../../src";

describe('ecs/systems/PhysicsSystem', () => {
    describe('.registerComponent()', () => {
        const physics = new PhysicsSystem();

        it.todo('Should register a Collidable component into the RenderSystem components list')
    })

    describe('.update()', () => { 
        it('Should detect collisions and trigger component updates accordingly', () => {

        });
    })
})